services:
  - type: web
    name: cybersentinel-pro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python core_engine.py
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8000
